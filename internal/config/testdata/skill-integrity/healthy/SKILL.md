---
name: healthy-skill
description: A complete skill used by the CheckSkillIntegrity tests.
---

# Healthy Skill

Read `references/guide.md` for the workflow, run `scripts/run.sh` to execute
it, and copy `assets/template.txt` when scaffolding output.

## Notes

All cited paths exist on disk, so this fixture must produce no warnings.
