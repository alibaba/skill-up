# No Frontmatter Here

This file does not open with a --- fence, so the whole document is treated as
body. It cites `references/present.md`, which exists, and
`references/orphan.md`, which is absent.
