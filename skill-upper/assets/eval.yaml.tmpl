schema_version: v1alpha1

environment:
  type: none                      # none | opensandbox
  # kwargs:                        # opensandbox 示例：base_url、extensions 等见 references/eval-yaml.md
  #   base_url: https://agent-sandbox.example.com
  # image: registry.example.com/your-org/sandbox-base:latest
  # workspace_mount: /workspace
  # env:
  #   TZ: UTC
  # setup_steps:
  #   - run: git config --global user.name "eval"

skills:
  - source: local_path
    path: .                       # 待测 Skill 目录
  # - source: local_path
  #   path: ../dependency_skill   # 其他 SKILL.md 所在路径

engine:
  name: claude_code               # claude_code | codex | qodercli（也兼容 qoder-cli）
  # model 可选；不填时引擎使用本地默认模型
  # model:
  #   provider: anthropic
  #   name: claude-sonnet-4-6
  #   base_url: ""

# mcp:
#   servers:
#     - name: github
#       mode: real
#       transport: http
#       config_ref: evals/fixtures/mcp/github.json

cases:
  files:
    - evals/cases/basic.yaml
  defaults:
    timeout_seconds: 120
    max_turns: 5
  # parallelism: 4                # 无状态用例可并行；也可用 skill-up run --parallelism N 临时覆盖

# benchmark:
#   enabled: true

report:
  formats: [json]
